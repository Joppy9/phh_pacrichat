extends layout.pug
block head
  title ログアウト
block content
  .container-fluid
    .row
      .col-md-6.col-md-offset-3
        form(action="/logout" method="post")
          h3  本当にログアウトしますか？
          a.navbar-brand.btn.panel.panel-default.panel-body.btn-default.btn-lg(href="/timeline") そのまま(Chatへ)
          input.btn-default.btn-lg(type="submit"  value="ログアウト") 